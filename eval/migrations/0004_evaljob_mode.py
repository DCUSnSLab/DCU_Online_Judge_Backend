# Generated by Django 5.2.5 on 2026-05-15 11:26

from django.db import migrations, models


def backfill_mode_from_force(apps, schema_editor):
    """기존 EvalJob row 의 mode 를 force 값으로 backfill.
    force=True → all, force=False → pending. 신규 row 는 default=pending.
    """
    EvalJob = apps.get_model("eval", "EvalJob")
    EvalJob.objects.filter(force=True).update(mode="all")
    EvalJob.objects.filter(force=False).update(mode="pending")


def noop_reverse(apps, schema_editor):
    pass


class Migration(migrations.Migration):

    dependencies = [
        ('eval', '0003_evalconfig_pair_workers_per_job'),
    ]

    operations = [
        migrations.AddField(
            model_name='evaljob',
            name='mode',
            field=models.CharField(choices=[('pending', 'pending'), ('all', 'all'), ('failed', 'failed')], default='pending', max_length=16),
        ),
        migrations.RunPython(backfill_mode_from_force, reverse_code=noop_reverse),
    ]
